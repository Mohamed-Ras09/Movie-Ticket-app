import "package:flutter/material.dart";

class SelectedMovie extends StatelessWidget {
  const SelectedMovie({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
